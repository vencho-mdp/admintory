export const state = () => ({
  suppliers: undefined
});

export const mutations = {
  increment(state) {
    state.counter++;
  }
};

export const actions = {
  increment(state) {
    state.counter++;
  }
};
